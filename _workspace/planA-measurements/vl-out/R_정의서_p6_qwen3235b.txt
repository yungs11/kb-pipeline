4. 단계별 상세 정의

업로드 → 파싱 → 문서 가드 → 청킹 → 적재 → 메타저장 → 그래프재구성 (적재 완료 KB는 언제든 검색 가능)
4.1 업로드
<table><thead><tr><th>수행 주체</th><th>현업(담당자)</th></tr></thead><tbody><tr><td>입력</td><td>처리할 원본 문서</td></tr><tr><td>처리</td><td>현업이 문서를 "나의 지식베이스"에 올린다. 업로드 시 각 건에 고유 식별값이 부여되어 이후 단계가 추적·관리된다.</td></tr><tr><td>산출물</td><td>접수 건(고유 식별값 부여)</td></tr></tbody></table>
4.2 파싱
<table><thead><tr><th>수행 주체</th><th>시스템(파서)</th></tr></thead><tbody><tr><td>입력</td><td>업로드된 원본 문서</td></tr><tr><td>처리</td><td>문서 확장자에 맞는 파서를 자동 선택해 시스템이 읽을 수 있는 형태로 변환한다. 일반 PDF는 페이지 단위, 스캔본·이미지는 글자 인식(OCR) 경로로 처리한다. 표는 구조(병합 셀 등)를 유지하고, 표·그림 등 비텍스트 요소도 의미를 보존한다.</td></tr><tr><td>산출물</td><td>변환된 문서(페이지·구조 정보 보존)</td></tr></tbody></table>